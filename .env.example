# Aucune variable d'environnement applicative requise pour ce projet.
#
# Railway injecte automatiquement :
#   PORT  →  utilisé dans le Procfile / startCommand (ne pas définir manuellement)
#
# Si vous ajoutez de futures intégrations (DB, API keys, etc.), listez-les ici :
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# SECRET_KEY=your-secret-key
